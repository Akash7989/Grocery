import { FileHandle } from "./file-handle.model";

export interface Product {
   title:String,
   price:String,
   discount:String,
   category:String,
   quantity:String,
   productImage:FileHandle[]

   
    

    
}